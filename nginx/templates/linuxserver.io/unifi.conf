server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name unifi.DOMAIN.TLD;

    # security
    include /configs/security.conf;

    # reverse proxy
    location / {
        proxy_pass https://DOCKER_CONTAINER_NAME:8443;
        include /configs/proxy.conf;
    }
}
