server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name documents.DOMAIN;

    # security
    include /configs/security.conf;

    # reverse proxy
    location / {
        proxy_pass http://HOST:80;
        include /configs/proxy.conf;
    }
}
